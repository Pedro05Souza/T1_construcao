services:
  backend:
    container_name: backend
    build: .
    command: poetry run fastapi dev src/t1_construcao/main.py --host 0.0.0.0
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    restart: on-failure:3
    env_file:
      - .env